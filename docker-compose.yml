services:
  schneaggchat_db:
    image: mongo:latest
    #restart: unless-stopped
    logging:
      driver: none
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password
    volumes:
      - ./mongodb_data:/data/db
    networks:
      - spring-mongo-network
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongosh localhost:27017/test --quiet
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 40s

  schneaggchat_db_gui:
    image: mongo-express:latest
    #restart: unless-stopped
    ports:
      - "8081:8081"
    environment:
      ME_CONFIG_MONGODB_ADMINUSERNAME: admin
      ME_CONFIG_MONGODB_ADMINPASSWORD: password
      ME_CONFIG_MONGODB_URL: mongodb://admin:password@schneaggchat_db:27017/
      ME_CONFIG_BASICAUTH: false
    depends_on:
      - schneaggchat_db
    networks:
      - spring-mongo-network

  schneaggchatv3server:
    build:
      context: .
      dockerfile: Dockerfile
    #restart: unless-stopped
    depends_on:
      schneaggchat_db:
        condition: service_healthy  # Wait for MongoDB to be healthy
    ports:
      - "8080:8080"
    environment:
      SPRING_DATA_MONGODB_URI: mongodb://admin:password@schneaggchat_db:27017/db?authSource=admin
      JWT_SECRET: "very_very_very_very_very_very_very_very_very_secret_key"
      DEFAULTACCOUNT_PASSWORD: "defaultpassword_verYsEcret1!"
      EMAIL: "testtest@gmail.com"
      EMAIL_PASSWORD: "verysecret"
    networks:
      - spring-mongo-network
    volumes:
      - gradle-cache:/root/.gradle
      - ./images:/app/images
      - ./images_messages:/app/images_messages

networks:
  spring-mongo-network:
    driver: bridge

volumes:
  gradle-cache: